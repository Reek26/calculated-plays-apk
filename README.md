# Calculated Plays — debug APK

Sideload build. App source stays in the private frontend repo (`7853a69`).

Cold start is an **app** flow: splash → login → research home. Not the marketing website.

Login POSTs JSON to `https://app.calculatedplays.com/api/auth/login` over native HTTPS (WebView `Failed to fetch` fix).

**Download (no GitHub login):**

https://github.com/Reek26/calculated-plays-apk/releases/download/android-debug-7853a69/calculated-plays-debug.apk
