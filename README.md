# Calculated Plays — debug APK

Sideload build. App source stays in the private frontend repo (`a94457d`).

Cold start is an **app** flow: splash → login → research home. Unfolded Fold uses a grouped side rail.

Login POSTs JSON to `https://app.calculatedplays.com/api/auth/login` over native HTTPS.

**Download (no GitHub login):**

https://github.com/Reek26/calculated-plays-apk/releases/download/android-debug-a94457d/calculated-plays-debug.apk
